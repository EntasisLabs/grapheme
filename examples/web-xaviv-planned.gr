query WebXavivPlanned {
  web.xaviv(query: "agentic runtime orchestration", max_results: 3)
  |> if $state.error == "" then core.echo(message: "xaviv provider is ready") else core.echo(message: $state.error)
}
