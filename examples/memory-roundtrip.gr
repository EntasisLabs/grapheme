import memory from "grapheme/memory"

query MemoryRoundtrip {
  set {
    key: "greeting",
    value: { message: "Hello From Memory Module" }
  }
  |> memory.store_context(key: $state.key, value: $state.value)
  |> memory.load_context(key: $state.key)
}
