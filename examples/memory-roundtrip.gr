import memory from "grapheme/memory"

query MemoryRoundtrip {
  set {
    key: "greeting",
    value: { message: "Hello From Memory Module" }
  }
  |> memory.store_context(key: $current.key, value: $current.value)
  |> memory.load_context(key: $current.key)
}
