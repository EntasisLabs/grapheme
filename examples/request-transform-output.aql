import HTTP from "grapheme/http"
import Core from "grapheme/core"

query RequestTransformOutputDemo {
  HTTP.get(url: "https://httpbin.org/get") {
    state { current }
  }
  |> Core.pick(
    fields: ["status", "method", "url"]
  ) {
    state { current }
  }
  |> Core.echo() {
    state { current pipeline }
  }
}
