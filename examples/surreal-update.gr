import surreal from "grapheme/surreal"

query SurrealUpdate {
  set {
    connection: "local",
    target: "person:demo",
    data: {
      active: false,
      tier: "platinum"
    }
  }
  |> surreal.update(
    connection: $state.connection,
    thing_or_table: $state.target,
    data: $state.data
  )
}
