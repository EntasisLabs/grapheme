import secrets from "grapheme/secrets"

query SecretsSignDemo {
  set {
    secret: "api_key",
    payload: { action: "deploy", env: "prod" }
  }
  |> secrets.sign_request(secret: $state.secret, payload: $state.payload)
}
