query Q on Any {
  core.trim(text: "  Canary Stage  ")
  |> core.lower(text: "$state.text")
  |> core.replace(text: "$state.text", from: "canary", to: "rollout")
  |> core.split(text: "$state.text", sep: " ")
  |> core.join(sep: "-")
  |> core.upper(text: "$state.text")
}
