# Legacy Examples

This folder keeps historical and exploratory examples that are no longer part of the canonical stdlib demo set.

Use these for reference, migration context, or experiments, but prefer the root-level `examples/*.gr` files for current demos and docs.
