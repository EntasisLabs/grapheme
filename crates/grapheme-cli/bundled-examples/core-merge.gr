import core from "grapheme/core"

query CoreMergeDemo {
  set { language: "grapheme", version: "0.1", mode: "dev" }
  |> core.merge(
    left: $current,
    right: { mode: "runtime-native", wasm: true }
  )
}
